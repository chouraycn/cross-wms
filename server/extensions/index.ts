// facade for server/extensions — consolidates extension public API.
// Re-created during engine import-graph repair (real impl: extension-loader.ts).
export { extensionLoader } from './extension-loader.js';
