// Runtime barrel for building auto-reply status messages.
// cross-wms: status-message module lives in ./status.js (not ../status/status-message.js).
/** Build the status message shown by auto-reply status commands. */
export { buildStatusMessage } from "./status.js";
