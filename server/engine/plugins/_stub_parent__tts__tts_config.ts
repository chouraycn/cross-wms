// === PENDING MIGRATION STUB ===
// Source: openclaw/src/tts/tts-config.ts (待迁移)
// Status: 结构化类型占位 stub — 类型为 Record<string, unknown> / no-op 函数实现
// 注：openclaw resolveEffectiveTtsConfig 解析 agent config 中的 effective TTS 配置
//      依赖 provider registry、tts directives、defaults
// 注意：当前文件同时声明了同名 type 和 const，保留兼容历史用法

export type resolveEffectiveTtsConfig = Record<string, unknown>;
export const resolveEffectiveTtsConfig = (): Record<string, unknown> => ({});
