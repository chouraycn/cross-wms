/** Provider catalog unified text. 移植自 openclaw/src/plugins/provider-catalog-unified-text.ts。
 * 降级策略：返回空。 */
export function projectProviderCatalogResultToUnifiedTextRows(params: unknown): unknown[] {
  void params;
  return [];
}
