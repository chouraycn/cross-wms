/** Provider contract public artifacts. 移植自 openclaw/src/plugins/provider-contract-public-artifacts.ts。
 * 降级策略：返回空。 */
export function resolveBundledExplicitProviderContractsFromPublicArtifacts(params: unknown): unknown[] {
  void params;
  return [];
}
