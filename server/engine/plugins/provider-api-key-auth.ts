/** Provider API key auth. 移植自 openclaw/src/plugins/provider-api-key-auth.ts。
 * 降级策略：返回 undefined。 */
export function createProviderApiKeyAuthMethod(params: unknown): unknown {
  void params;
  return undefined;
}
