/**
 * Formats provider authentication choices exposed by plugin setup flows.
 * 移植自 openclaw/src/plugins/provider-auth-choice.ts。
 * 降级策略：依赖项未移植时，函数体降级为返回默认值或抛出 not implemented；
 * 类型定义保留形状供下游引用。
 */

export type ApplyProviderAuthChoiceParams = unknown;

export type ApplyProviderAuthChoiceResult = unknown;

export type PluginProviderAuthChoiceOptions = unknown;

export const testing: unknown = undefined;





