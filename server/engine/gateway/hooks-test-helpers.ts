// 移植自 openclaw/src/gateway/hooks-test-helpers.ts
// 注意：本文件为测试基础设施 stub，仅用于占位，不包含实际测试逻辑。

export function createHooksConfig(...args: any[]): any {
  return undefined;
}

export function createGatewayRequest(...args: any[]): any {
  return undefined;
}
