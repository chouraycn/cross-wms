// 移植自 openclaw/src/gateway/test-with-server.ts
// 注意：本文件为测试基础设施 stub，仅用于占位，不包含实际测试逻辑。

export async function withServer(...args: any[]): Promise<any> {
  return Promise.resolve(undefined);
}

export function installConnectedControlUiServerSuite(...args: any[]): any {
  return undefined;
}
