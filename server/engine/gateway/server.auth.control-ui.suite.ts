// 移植自 openclaw/src/gateway/server.auth.control-ui.suite.ts
// 注意：本文件为测试基础设施 stub，仅用于占位，不包含实际测试逻辑。

export function registerControlUiAndPairingSuite(...args: unknown[]): unknown {
  return undefined;
}
