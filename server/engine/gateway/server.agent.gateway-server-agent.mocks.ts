// 移植自 openclaw/src/gateway/server.agent.gateway-server-agent.mocks.ts
// 注意：本文件为测试基础设施 stub，仅用于占位，不包含实际测试逻辑。

export const registry: any = undefined;

export function setRegistry(...args: any[]): any {
  return undefined;
}
