// 移植自 openclaw/src/gateway/test-http-response.ts
// 注意：本文件为测试基础设施 stub，仅用于占位，不包含实际测试逻辑。

export function makeMockHttpResponse(...args: any[]): any {
  return undefined;
}

export function makeMockHttpReqRes(...args: any[]): any {
  return undefined;
}

export async function readClientResponseBody(...args: any[]): Promise<any> {
  return Promise.resolve(undefined);
}
