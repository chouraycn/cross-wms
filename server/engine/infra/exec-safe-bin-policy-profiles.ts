// Re-export of the canonical implementation to eliminate duplicated code (jscpd consolidation).
// 本文件原先未被任何模块引用（死代码，含 @ts-nocheck）；规范实现位于 ./exec-approvals/ 子目录。
export * from "./exec-approvals/exec-safe-bin-policy-profiles.js";
