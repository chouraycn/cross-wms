export const taskflowSkill = {
  name: "taskflow",
  description: "Coordinate multi-step detached tasks as one durable TaskFlow job with owner context, state, waits, and child tasks.",
};

export default taskflowSkill;
