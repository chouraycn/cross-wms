// 移植自 openclaw/src/channels/plugins/status.ts

export async function buildChannelAccountSnapshotFromAccount(..._args: any[]): Promise<any> {
  return Promise.resolve(undefined);
}

export async function buildReadOnlySourceChannelAccountSnapshot(..._args: any[]): Promise<any> {
  return Promise.resolve(undefined);
}

export async function buildChannelAccountSnapshot(..._args: any[]): Promise<any> {
  return Promise.resolve(undefined);
}
