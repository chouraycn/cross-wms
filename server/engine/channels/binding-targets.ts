// 移植自 openclaw/src/channels/plugins/binding-targets.ts

export async function ensureConfiguredBindingTargetReady(..._args: any[]): Promise<any> {
  return Promise.resolve(undefined);
}

export async function resetConfiguredBindingTargetInPlace(..._args: any[]): Promise<any> {
  return Promise.resolve(undefined);
}

export async function ensureConfiguredBindingTargetSession(..._args: any[]): Promise<any> {
  return Promise.resolve(undefined);
}
