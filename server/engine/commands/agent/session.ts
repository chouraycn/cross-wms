// Public command re-export for session key helpers used by CLI and gateway dispatch paths.
export {
  buildExplicitSessionIdSessionKey,
  resolveSessionKeyForRequest,
} from "@openclaw-src/agents/command/session.js";
