# 2026-07-22 工作日志

## 软件完整度重新评估（第 3 轮）

### 关键发现
- **类型健康严重恶化**：server tsc 从 07-20 的 9 错误暴增到 **1894 错误 / 216 文件**
  - 归因：162 文件是**已提交代码**（工作区无改动），54 文件是工作区改动
  - 即最近 commit 链（"补全CLI+国内社交通道"）在未过 `server/tsconfig.json` 门禁的情况下合入了大量半成品移植代码
  - 错误集中在 plugins（gateway-startup-plugin-ids 173/runtime 111/install 87/hooks 74）和 agents（thinking 52/index 45），多为 TS2305（引用不存在的导出）/TS2339（属性不存在）
- **推翻前次前提**：07-14~07-19 评估中"已提交代码 tsc 0 错误"的结论不再成立

### 我们的修复状态
- SSE/logger 5 文件已提交，`git diff HEAD` 为空
- `sseStreaming + chatService` 测试 46/46 绿

### 工作区状态
- 318 变更（148 M + 170 ??），版本 1.7.129
- 170 未跟踪文件是 skills 可观测性功能开发（SkillHealthDashboard/UsageAnalytics/DocQuality/DependencyGraph），已接入主干（skills-api.ts + App.tsx）
- 改动量 +11,860/−2,286

### 安全 P0
- 部分缓解：host-env-security/path-guards 已接入主干（07-19 半成品已升级）；有 sandbox commit
- 核心仍在：HTTP 监听无显式 host（server/index.ts:635）、chat 路由无鉴权

### 判定
- 🔴 No-Go 维持，因类型健康恶化而风险升级
- 落盘：`deliverables/gstack/reassessment-software-2026-07-22.md`

### 教训
- server 有独立 `server/tsconfig.json`（Jul 16 新建），根 tsconfig 只 include `src`/`shared`
- 之前跑的 `tsc --noEmit`（无 -p）只查前端，server 真实类型健康需 `tsc --noEmit -p server/tsconfig.json`
- commit 前必须跑 server 独立门禁，否则半成品移植代码会带类型债进主干

## 第 4 轮复析（晚间 20:33 实测）
- **状态剧变**：server tsc **1894 → 0 错误**、frontend 0 错误，测试 46/46 绿（全在工作树，359 文件未提交：157 M + 202 ??，v1.7.129）
- **根因新发现（最关键）**：类型门禁"配置存在但零 enforcement"
  - `.git/hooks/pre-commit` 为空 → husky 未装进 git（`package.json` 有 `prepare: husky` 但没生效）
  - 提交直推 main（无 PR merge commit）→ `pr-quality-gate.yml:135` 的 server tsc 被绕过
  - 即 1894 错误进主干不是漏跑，而是 main 上没有任何强制关卡
- **阻塞项净变化**：#5 路径穿越已缓解（security-path.ts 接入）；类型项红转绿（树内）；**新增"门禁虚设"根因项**；#3 chat鉴权 / #4 0.0.0.0绑定 / #6 双层存储 / #7 跨境 / #8 回滚 仍硬阻塞
- **下一步方向**：P0-A 焊死门禁（npx husky install + main 分支保护禁直推）；P0-B 按序清 #4→#3→#6→#7→#8；P0-C 359 文件拆 PR；P0-D 完成 A+B 后出第5轮（可降 🟡）
- 落盘：`deliverables/gstack/reassessment-software-2026-07-22-r2.md`
