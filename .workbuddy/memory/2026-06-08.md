## 2026-06-08

### DMG v1.3.8 构建并发布
- 修复了构建脚本版本号不再卡死的问题：默认自动 bump patch（`--no-bump` 可跳过）
- `build-dmg-pywebview.sh`：BUMP_TYPE 默认 `"patch"`，支持 `--no-bump` 保持不变
- 构建：前端 3098 modules (7.62s)，后端 542.7KB esbuild，PyInstaller 6.20.0/Py 3.14.3/arm64 → DMG 57MB
- GitHub Release：先删旧资产 → Python urllib 上传 → DMG + release.json 均 201 成功
- 发布地址：https://github.com/chouraycn/cross-wms/releases/tag/v1.3.8

### ModelManagement 恢复
- `git checkout HEAD -- src/components/Settings/tabs/ModelManagement.tsx` → 恢复到旧版简洁 List 视图
- 保留添加/编辑模型弹窗功能，去掉了新版表格/Provider卡片等重叠 UI
