# 2026-06-16

## 紧急修复：代码版本回退恢复

**问题**：package.json 版本从 1.5.79 回退到 1.5.17，用户之前的多版本修改全部丢失。

**根因**：`origin/main` 被 reset 到旧版本（commit 15519cc9），导致 v1.5.28~v1.5.79 共 28 个提交脱离 main 分支。虽然 tag 还在本地，但代码不在 HEAD 上。

**恢复操作**：
1. 创建备份分支 `backup/1.5.17-current` 保留当前 HEAD
2. `git reset --hard v1.5.79` 恢复代码到 v1.5.79 tag
3. `git push --force origin main` 覆盖远程

**结果**：代码已恢复到 v1.5.79 状态，origin/main 已同步。备份分支 `backup/1.5.17-current` 保留以备需要时查看。

## v1.5.84 修复发布

用户报告恢复后的 v1.5.75 有 4 个问题：

1. **应用重命名 CrossWMS → CDF Know**：v1.5.83 的变更，当前代码已部分完成，需合并完整重命名
2. **对话框自动换行问题**：MarkdownRenderer 缺少 overflowWrap/wordBreak，代码块长行溢出
3. **DMG 窗口拖拽**：确认 WindowDragBar(38px) + WebkitAppRegion:drag 方案正确，顶栏拖动+内容区文字选择是预期行为
4. **历史对话丢失**：localStorage 键名从 cross-wms-chat-sessions 改为 cdf-know-clow-chat-sessions 后未做迁移

**修复方案（v1.5.84）**：
- ChatContext.tsx 和 useChatSession.ts 添加旧键自动迁移逻辑
- MarkdownRenderer.tsx 添加 overflowWrap:break-word + code block overflowX:auto
- providerApiKeyUrls.ts 从空文件重建为完整映射
- 从 v1.5.83 复制：P0技能系统文件、modelsStore（24+内置模型）、sessionStore、CI/CD修复
- 无法直接 merge/cherry-pick v1.5.83（两个分支从 v1.5.10 分叉，32个文件冲突）

**已提交**：v1.5.84 tag + main 已推送 origin
