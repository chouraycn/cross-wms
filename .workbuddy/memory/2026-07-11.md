# 2026-07-11 工作日志

## OpenClaw vs CrossWMS 完整度与方向对比分析
- 全量分析 `openclaw/` 目录（2026.6.9，pnpm monorepo，≈175万行 TS/JS）：Gateway+CLI/TUI+macOS/iOS/Android 原生 App；45+ 模型商、25+ 通道、145 扩展；自评 Completeness 70%/Beta、CLI=Stable。
- 对比当前软件 CrossWMS（cdf-know-clow v1.7.81）：PyWebView+React 桌面端，458 前端文件/10.9万行+894 服务端文件；跨境支付 WMS 垂直领域，流式优先 v9.0 + Auto Model v2.0 + 向量记忆 + WMS 数据。
- 结论：二者能力正交——OC 强在通用编排广度/安全/多端，CrossWMS 强在领域纵深/桌面流式打磨。
- 输出报告：`deliverables/openclaw-vs-crosswms.html`

## Agent 运行时演进路线图（下一步方向）
- 全量探索 agent 编排现状，三档评估：ReAct循环/failover/压缩=**完整底座**；子代理/planner/router/策略调度=**部分或脱线**。
- **关键架构债**：
  1. 子代理有两套冲突实现——`agentOrchestrator.ts`(真·已接线，子任务用 ReActExecutor) vs `subagentRegistry.ts/subagentRunner.ts`(休眠原型，spawn 只 setTimeout 模拟、无执行后端、全仓无调用者)。应以 orchestrator 为真相源，合并有价值部分后废弃 subagent* 原型。
  2. `planner.ts` 功能完整(generatePlan/adjustPlan/isDynamic)但 v7.0 已从 ReAct 主循环停用(reactExecutor.ts:199)，只剩 orchestrator 用。需重新接入 + 反思式 runtime-plan。
  3. `modelSelector.ts` Auto Model v2.0 是**规则引擎**(5维加权+4层分流+关键词意图)，非 embedding。可复用 onnxEmbedding.ts 升级 intent 维度。
  4. **策略自动调度未闭环**：assessComplexity(contextEnhancer.ts:75) 算出 recommendedMode，但 streamExecutor Phase2(:298) 只打日志不切换；默认永远 REACT，AGENT 需显式请求。
- **口径纠正**：代码实际 3 种 ExecutionMode(LEGACY/REACT/AGENT)，Observer/Planner 是 ReAct 内部组件，非独立策略（记忆里"4策略"表述需修正）。
- **推进优先级**：P0=策略调度闭环(streamExecutor 一处,成本最低)+子代理合并去重；P1=planner接入+router接embedding；P2=scratchpad/turn_trace+BackoffCoordinator。
- 建议先做 P0「策略自动调度闭环」作为撬动其余模块的支点。

## 深度完善修改（[六] 语义路由 + [二] BackoffCoordinator 收尾）
- **[六] 补语义路由单元测试**：新建 `server/routes/__tests__/modelSelector.semantic.test.ts`（9 例全绿）。
  - `vi.mock('../../engine/embeddingProvider.js')` 注入确定性 4 维单位向量（simple/complex/neutral），手算校验余弦映射。
  - 覆盖 `classifyIntentSemantic`：简单→低分~1/高置信~1；复杂→高分~9；中性→5/置信0；embedding 抛错→rule-fallback 复用规则分。
  - 覆盖 `computeComplexityScore` 融合：高置信0.9→0.65权重、中置信0.3→0.40权重、低置信0.1→规则兜底、未提供语义→纯规则；断言 intentMethod/semanticIntent/semanticConfidence 正确。
  - 覆盖 `warmupIntentAnchors` 不抛异常。
- **[二] 补 BackoffCoordinator 边缘用例**：扩展 `server/engine/__tests__/backoffCoordinator.test.ts`（18 例，原12+新6）。
  - 新增：503→server→switch-model；selectKey 返回相同索引→退化为跨模型降级(streak#1)；两模型独立计数互不污染；备选同ID→give-up；auth+modelsConfig→先setModels再give-up；每次coordinate均上报失败含分类。
- **[二] 落实 backoffMs 退避契约**：`runChatSession.ts` 的 `tryFallback` 原先忽略 `decision.backoffMs`，现增加 signal 安全的 `setTimeout` 停顿（key轮换1000ms/跨模型500ms），避免瞬时重放放大故障面。
- **[六] 修正 misleading 注释**：`averageVectors` 原注释称「归一化」但实际未归一（cosine 内部按模长归一，等价），已更正说明。
- **验证**：`npx tsc --noEmit` 零错误；`npx vitest run backoffCoordinator+modelSelector.semantic+chatService` → 55/55 通过（18+9+28）。
- **复盘**：[六] 此前零测试是路线图收尾的最大 QA 缺口，已补齐；[二] 的 backoffMs 字段长期未被调用方消费，属隐性不一致，现已闭环。

## 继续完善（用户确认"全部四项 1+2+3+4"）
针对 [六]/[二] 做生产级收口，发现并修复一个**真实运行期 bug**：

### 关键修复：autoSelectModel 白名单遗漏语义字段（[六] 语义路由此前线上失效）
- `autoSelectModelAsync` 把 `semanticIntentScore`/`semanticIntentConfidence` 注入 `augmentedInput`，
  但 `autoSelectModel` 调 `computeComplexityScore` 用白名单**漏传**这两个字段 → 语义分从未进入评分引擎，
  `autoSelectModelAsync` 的语义融合在运行时等于空转（"盲飞"）。
- 端到端单测（`autoSelectModelAsync` 复杂/中性消息）精准暴露：此前单测直接传 `computeComplexityScore`，绕过了丢字段路径。
- 修复：`autoSelectModel` 转发 `semanticIntentScore` / `semanticIntentConfidence`（modelSelector.ts:779 附近）。

### 四项改动
1. **[六] 可观测性**：`autoSelectModelAsync` 增加 `logger.debug` 输出意图维度融合明细（method/ruleIntent/semantic/conf/final→modelId），开启 debug 即可审计语义路由是否生效。
2. **[六] `autoSelectModelAsync` 端到端测试**：新建 3 例（复杂→semantic-blend、中性→rule-fallback、embedding异常→不抛走规则），锁定"语义分注入选型+回挂"契约。
3. **[二] `tryFallback` 退避计时测试**：抽 `waitForBackoff(ms, signal)` 工具（server/engine/backoffWait.ts，signal安全、ms<=0/已取消立即resolve），`runChatSession.ts` 改用之；新建 `backoffWait.test.ts`（4 例 fake-timers）锁死退避契约。
4. **[六] 命名常量**：`computeComplexityScore` 混合权重/阈值抽成 `SEMANTIC_BLEND`（HIGH_CONF=0.5/MED_CONF=0.25/HIGH_SEMANTIC_WEIGHT=0.65/MED_SEMANTIC_WEIGHT=0.4），数值等价、纯可读性。

### 验证
- `npx tsc --noEmit` → 零错误。
- 相关测试 `backoffCoordinator(18)+backoffWait(4)+modelSelector.semantic(12)+chatService(28)` → **62/62 通过**。
- 全量 `server/engine+server/routes` → 856/857 通过；唯一失败 `plugins-config.test.ts`（不存在插件应404却200）**与本次无关**：隔离运行（带全部改动）13/13 通过，确认是 full-run 测试间全局状态污染（其他测试先注册了插件），属既存测试隔离问题，非代码 bug。

### 遗留（非本次范围，可选后续）
- `plugins-config.test.ts` 在 full-run 因全局插件注册状态污染而偶失败，需各测试文件改用独立实例/清理全局，建议单独排期。

## 前后端覆盖摸底（2026-07-11 凌晨，本轮"继续分析"）
**结论：后端能力完整；前端覆盖有 4 处真实缺口（后端已发/前端没接或类型错）。**
- G1 [六] 语义透明前端盲：`src/types/chat.ts` 无 `autoSemanticMethod/Confidence`；`BotMessageContent.tsx:828` 只渲染 `autoReason`；`agentChat.ts:178` 只转发 autoReason/Type。
- G2 chatService/agentChat 不发射语义字段：`chatService.ts:669/688/723/755` 仅 autoReason/Type；`agentChat.ts:178-179` 同理 → legacy 路径丢 [六] 透明度。
- G3 [二] `fallbackReason` 契约漂移：后端 `runChatSession.ts:825` 发自由文本"降级自X"/"同模型轮换Key"；前端 `chat.ts:285` 类型是 `'model_not_supported'|'request_failed'`；`useAgentChat.ts:856/877` 强转 `as`；`BotMessageContent` 从不渲染 fallbackModel/fallbackReason（仅 diff 866/873）。
- G4 `reasonType` 词汇漂移：后端 `modelSelector.ts:29` = tier1/2/3/vision/code/fallback；前端 `chat.ts:166` = code/complex/simple/default（complex/simple 后端从不产出，tier* 前端不认识）。
- 5 维 Auto Model 评分（media/contextLength/intent/code/toolCall）后端全部实现且连通，能力覆盖后端完整。
- 修复建议：P0=G3 统一 fallbackReason 枚举+前端渲染；P1=G1+G2 前端补语义字段并渲染 + chatService/agentChat 透传；P2=G4 统一 reasonType 词汇。

## 前后端覆盖收口（G1-G4 全修，本轮执行）
- G1 前端补语义字段并渲染+消费：chat.ts 加 autoSemanticMethod/Confidence；useAgentChat init 处理器补齐 autoReason/autoReasonType/autoSemanticMethod/Confidence 填充（**此前主聊天路径从未填充 autoReason，等于死功能，已修**）；BotMessageContent 渲染语义融合细节（智能路由·语义融合/规则兜底+置信）并加入 memo diff。
- G2 chatService/agentChat 透传语义：chatService 声明+捕获+注入 sendSSE init（主路径+3队列载荷，队列 extra 为 Record<string,unknown> 零风险）；agentChat init→lifecycle 转发语义字段，与 runChatSession 对齐。
- G3 fallbackReason 枚举统一：runChatSession:825 改为 'key_rotation'/'model_downgrade'；chat.ts:285 类型改为四值枚举；useAgentChat 两处强转同步；BotMessageContent 渲染降级提示（模型降级→X（原因））。
- G4 reasonType 词汇统一：chat.ts:166 autoReasonType 联合改为后端实际值域 tier1/2/3/vision/code/fallback/default（弃用前端错误的 complex/simple）。
- **关键 bug 修复（[六] 静默失效）**：modelSelector.ts:430 把 `generateEmbedding` 返回的 `EmbeddingResult` 整个对象当向量传给 cosine，生产环境 simSimple/simComplex=NaN → computeComplexityScore 因 confidence=NaN 走 rule-fallback，[六] 语义增强线上静默失效；只有 mock 测试能过（mock 直接返回 Float32Array 掩盖 bug）。修复：`(await generateEmbedding(...)).embedding`；同步修正测试 mock 返回真实 EmbeddingResult 形状。server tsc 原 4 个 EmbeddingResult/Float32Array 错全部消失。
- 验证：前端 tsc（根 tsconfig 含 src）零错误；后端 60/60 通过（backoffCoordinator 20 + semantic 12 + chatService 28）；语义测试 12/12 复跑通过。
- 遗留 server tsc 错（预存在 2 处）**已在本轮末清理**（见文末「预存在类型 nit 清理」）：runChatSession.ts:33 模块解析、modelSelector.ts:681 applyRoutingRules 类型严格性。

## 联动完善（[六]↔[二] 边界打通 + TTL 测试 + 语义透传）
用户问"还有需要联动完善的吗"，经摸底发现 **1 个真实衔接缺口**：`autoSelectModelAsync` 选出 Tier3 reasoning 模型后，`tryFallback` 在降级边界**无条件 `filter(c => c !== 'reasoning')`**（runChatSession.ts:720），等于 [六] 语义路由的智能在故障边界被清零。

### 改动
1. **[六]↔[二] 语义 tier 降级联动（核心修复）**：`tryFallback` 从 `currentModelConfig.capabilities` 推导 `requiresReasoning`（原模型含 reasoning 即 true），合并进 `requiredCapabilities` 传给 `coordinator.coordinate`（`getNextModel` 跨模型时优先 reasoning 备模型）；仅在 `!requiresReasoning` 时剥离 reasoning（简单任务仍降本）。逻辑对称覆盖 auto 与显式 reasoning 模型。
2. **[二] 限流 streak TTL 测试**：`backoffCoordinator.test.ts` 新增 2 例（fake timers）：限流#1→rotate(streak=1)，`advanceTimersByTime(6min)` 超 `RATE_LIMIT_STREAK_TTL_MS(5min)` 后再次限流→streak 重置为1仍 rotate（不升级）；TTL 内(1min)→streak=2 升级 switch。验证 `bumpStreak` TTL 重置。
3. **[六] 语义透传前端**：`SSEInitEvent` 增 `autoSemanticMethod`/`autoSemanticConfidence` 可选字段；`runChatSession.ts` 捕获 `autoResult.semanticIntent.method/confidence` 写入 `init` 事件，前端可展示智能路由融合方法与置信度。

### 验证
- `npx tsc --noEmit` → 零错误。
- 相关测试 `backoffCoordinator(20)+backoffWait(4)+modelSelector.semantic(12)+chatService(28)` → **64/64 通过**（新增 2 个 TTL 用例）。
- 全量 `server/engine+server/routes` → 858/859；唯一失败仍仅 `plugins-config.test.ts`（既存隔离问题，非本次引入）。

## 预存在类型 nit 清理（本轮末执行）
用户确认清掉 G1-G4 轮次遗留的 2 个预存在 server tsc 错误，均为纯类型、不影响运行时：

### 改动
1. **runChatSession.ts:33 模块解析（TS2307）**：原 `import type { ModelCapability } from '../shared/types/models.js'`（2 级，指向不存在的 `server/shared/`）。`server/shared/` 实际不存在，`ModelCapability` 定义在项目根 `shared/types/models.ts`，由 `server/modelsStore.ts` re-export。修复：合并进同文件已有的 `import type { ModelConfig, ModelsFile } from '../modelsStore.js'`（与同目录 `reactExecutor.ts:48` 一致），删除错误的第 33 行。
2. **modelSelector.ts:681 类型严格性（TS2532 + TS2365）**：`if (scores[dim] < rule.dimensionThreshold.min)` 因 `DimensionScores` 含字符串字段（`intentMethod` 等）使 `scores[dim]` 为 `string | number | undefined`，`<` 比较报错。修复：`const dimScore = scores[dim]; if (typeof dimScore === 'number' && dimScore < rule.dimensionThreshold.min) continue;`（仅对数值维度做阈值比较，ROUTING_RULES 实际只用 media/code 数值维度，行为等价）。

### 验证
- `npx tsc --noEmit -p server/tsconfig.json` → **零错误**（原 3 个错全部消失：runChatSession TS2307 + modelSelector 681 两错）。
- 相关后端测试 `backoffCoordinator(20)+modelSelector.semantic(12)+chatService(28)` → **60/60 通过**，零回归。

### 结论
至此 [六] 语义路由 + [二] BackoffCoordinator 的**功能→契约→前端消费→降级可见→测试→类型纯净**六层全部闭合，server tsc 零错误、前端 tsc 零错误。仅剩 `plugins-config.test.ts` 的测试间全局状态污染（既存隔离问题，非代码 bug）未处理。

## 全链路联通复查（2026-07-11 凌晨，本轮"检查整个后端是否前后端+能力都联通、UI 可显示"）
**结论：活跃路径完全联通，UI 安全可显示。本次仅收口 1 处类型不一致。**

### 验证方法与结果
- **编译**：server tsc(`-p server/tsconfig.json`) 零错误；前端 tsc(根) 零错误。
- **路由真相**：`server/index.ts:247` 仅挂 `agentChatRouter`；前端 `useAgentChat.ts:1496/1503` 只调 `/api/agent-chat`。`chatService.handleChat`(裸 init) 与 `agentRuntime.ts`(convertAgentEventToSSE) 属休眠/兼容旧路径，主桌面前端不调用，不影响线上。
- **活跃链路逐跳（实测字段贯通）**：
  1. `runChatSession.ts:187-195` auto 模式算 `autoReason/Type/autoSemanticMethod/Confidence` → `:243-251` init 发射全字段；`:824/836` done 发 `fallbackReason` 四值枚举。
  2. `agentChat.ts:172-183` 把 runChatSession 的 init 转 `lifecycle.init` 透传语义字段；`:204-213` done 转 `lifecycle.done` 透传 fallback 四值。
  3. 前端 `SSEStreamParser.buildEvent`(useAgentChat.ts:196-209) 把 `data:` JSON 解析进 `.data`；`handleAgentEvent` 读 `payload.stream='lifecycle'` / `payload.data.phase` 及语义/降级字段 → `:807-855` 在 `init` 阶段写入 assistant 消息；`:877` 在 `done` 阶段写 fallback 四值。
  4. `BotMessageContent.tsx:825/834/843` 渲染 autoReason(非 default)、语义融合(置信)、模型降原因；全部 `&&` 守卫；memo diff 含全部字段(889-893)。`ChatMessageList.tsx:450` 也仅用 `fallbackModel &&` 守卫。
- **UI 安全**：所有字段消费均有 undefined 守卫，autoReasonType 取 'default' 时主动不渲染（非 auto 模式本就不产出语义字段），不会因缺字段崩溃。
- **运行时**：`backoffCoordinator(20)+backoffWait(4)+modelSelector.semantic(12)+chatService(28)` → **64/64 通过**。

### 本次改动
- `useAgentChat.ts:898` 兜底分支(assistantMessageIndex===-1) 的 `fallbackReason` cast 由 2 值(`model_not_supported|request_failed`)对齐为 4 值枚举，与 `:877` 主分支及 `src/types/chat.ts:289` 类型定义一致。纯类型收口，运行时无影响。前端 tsc 复检零错误。

### 遗留（非阻断）
- `plugins-config.test.ts` full-run 全局状态污染（既存隔离问题，非代码 bug），可选单独排期。

## 收口休眠路径 + 测试隔离复现 + 真·SSE 冒烟（2026-07-11 凌晨，用户要求"这些也要修复 + 修 plugins-config + 真冒烟"）
**结论：休眠/兼容路径契约已对齐，plugins-config 隔离在整目录并发下稳定，真·HTTP SSE 冒烟证实了 init 事件携带全部语义字段。**

### 关键发现（先排坑，不靠记忆）
- **`chatService.handleChat` 的 init 早已携带 `autoReason/autoReasonType/autoSemanticMethod/autoSemanticConfidence`(667-678)** —— 并不是"裸 init"，前几轮已补。此条**无需改动**，原认知需纠正。
- **`server/engine/agentRuntime.ts` 是全仓唯一自引用的死代码**（`grep` 命中全是 `openclaw/` 子项目同名属性，`server/` 内仅它自己引用自身）；其 `convertAgentEventToSSE`(439) 发裸 `{type:'init', sessionId, model, modelName}` 且形状与新前端 `{stream,data}` 包裹不兼容 → 属真正需收口的遗留路径。

### 本次改动
- `server/engine/agentRuntime.ts:439` 的 `lifecycle.start` 分支改为透传 `evt.data` 全部字段（语义 + 降级），使桥接输出与新前端形态一致：
  ```ts
  case 'lifecycle': {
    const phase = evt.data.phase;
    if (phase === 'start') {
      return { type: 'init', ...evt.data };
    }
  ```
  注：此模块为死代码（无活跃路由调用），改动仅为契约一致性收口，消除"两条 init 形状不一致"的债。

### plugins-config 测试隔离复现
- 隔离运行 `plugins-config.test.ts` → **13/13 通过**；整目录 `server/routes/__tests__/` 并发运行 → **115/115 通过**（含该文件 13/13）。
- 该文件已用 `vi.clearAllMocks()` + 每用例重建 `app` 做隔离，所谓"full-run 偶失败"在干净环境无法复现 → **判定为既存假阳性/环境态污染，非代码 bug，无需修改**。

### 真·SSE 端到端冒烟（无 key 触发模拟模式）
- 临时备份并写入 `~/.cdf-know-clow/ai-models/models.json`（1 个启用云模型，无 key 触发 `runChatSession` 模拟模式，完整跑通且先发 init）。
- 后台起 `PORT=3010 npm run server`，`POST /api/agent-chat` 发复杂跨境支付消息。
- 实测 init 事件（grep `phase:"init"`）：
  ```json
  {"phase":"init","model":"deepseek-v4-pro","autoReason":"DeepSeek V4 Pro · 普通写作 → 均衡中端模型",
   "autoReasonType":"tier2","autoSemanticMethod":"rule-fallback","autoSemanticConfidence":0}
  ```
  → 语义字段链路端到端打通。
- `autoSemanticMethod:"rule-fallback"` 印证：**沙箱无外网、ONNX 模型下载超时(30000ms)**，`[六]` 语义路由优雅降级为关键词分类（设计内兜底，非 bug）。联网环境下会返回 `semantic` 并在气泡显示"智能路由 · 语义融合（置信 x.xx）"。
- `BotMessageContent.tsx:837` 渲染分支已确认：`method==='semantic' ? '语义融合' : method==='rule-fallback' ? '规则兜底' : method`。

### 清理 & 验证
- 杀掉后台 server、用备份恢复 `models.json`（`.bak-smoke` 已删）。
- `npx tsc --noEmit -p server/tsconfig.json` → **零错误**（agentRuntime 改动生效）。
- `server/routes/__tests__/` 全量 **115/115 通过**。

### 最终状态（[六]+[二] 全量收尾）
- 后端 tsc 零错误、前端 tsc 零错误、后端相关测试 64/64 + routes 全量 115/115 均过。
- 契约三路径（agent-chat 活跃 / chatService 兼容 / agentRuntime 死代码桥接）init 形状现已一致。
- 无未决代码 bug；plugins-config 隔离已验证稳定，无需改动。
