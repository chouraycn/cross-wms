# 2026-07-11 工作日志

## 死代码「全做」收口（前端 46 + 后端 179 全量接入）

用户指令："全做" → 推进剩余前端簇 + 后端 179 全部接入（非删除，完善能力）。

### 执行方式
- 并行 5 个前端簇代理（Workflow/Goals/Automation/Matching/Wiki）+ 1 个后端 triage 调研代理；后端再并行 5 个接入代理（Group A 胶水 / cron / tool-plan+embeddings+plugin-sdk / live 路径×2 / reports+security-audit+config）。
- 统一约束：**不删代码、不碰共享 barrel(src/services/index.ts、src/types/*)、不碰 App.tsx（并行期）**；每个代理改完跑 tsc 零错误；后端挂载点由主代理**集中一次性**加进 server/index.ts（避免并发改同文件冲突）。

### 前端（46 候选 → 全量处理）
- Memory/Git（前序已完成）。
- Workflow：6 节点组件接进 WorkflowEditor 属性面板（contract 100% 正确）。
- Goals：GoalsPage → GoalPanel（原 page 用旧 service 模型，坏；组件契约正确）。
- Automation：AutomationPanel(CronBuilder) 接进 AutomationPage + 新增 cronAdapter.ts 做 rrule↔cron 双向转换（前端引擎只认 RRULE）。
- Matching：SkillMatchResult + MatchFeedbackWidget 接进 MatchingPage；修正 matchingApi 契约。
- Wiki：WikiPage → WikiPanel（契约 100% 正确，仅 WikiPanel 的 loadRecentEntries 兜底 total）。
- 散件：仅 `BrowserSnapshotPanel` 真正接进 BrowserPage；其余 15 项经核实为**重复实现/已用桶/无消费者**（ShipmentHeatmap 与活 Heatmap 重复、LSP CompletionDialog 与 LspServersPage 内联重复、Layout/TopBar/SidebarToggle 死、Marketplace CategorySidebar 重复、Soul index 桶、WebhookPanel 重复、FadeIn 被 usePageFadeIn 取代、hooks/constants/types 死、services/webhook 死）——正确留文档，不强行接。
- `SkillWorkshopPage`：已写完但**未挂路由**；补 lazy 导入 + `<Route path="/skill-workshop">`（/api/skill-workshop 后端路由已存在）。
- **验证**：前端 `npx tsc --noEmit -p tsconfig.json` 全量 **exit 0 零错误**。

### 后端（179 候选，triage 结论：0 个 knip 误报）
- **Group A（胶水，路由已挂载）**：context-engine 5 子模块（memoryBudget/contextProjection/promptCache/quarantineHealth/runtimeSettings/subagentLifecycle/transcriptRewrite 中 5 个真死）接进 contextEngineService + routes/contextEngine（新增 ~25 端点）。memory/agents/dao/automation+projects/prompts 经核实为**替代后端/重复薄板/无消费点** → 留文档不接（避免双后端冲突）。
- **Group B（新路由）**：新增 6 个路由文件并挂载——
  - `/api/cron`（routes/cron.ts，封装 engine/cron/*，前端 CronBuilder 已就绪）
  - `/api/tool-plan`（routes/toolPlan.ts，封装 engine/tool-plan/*）
  - `/api/embeddings`（routes/embeddings.ts，封装 engine/embedding-providers/*）
  - `/api/plugin-sdk`（routes/pluginSdk.ts，封装 engine/plugin-sdk/*）
  - `/api/reports`（routes/reports.ts，封装 services/reportService.ts）
  - `/api/security-audit`（routes/securityAudit.ts，封装 services/runtimeSecurityAudit.ts）
  - contextEngineService 增强进已挂载的 /api/context-engine。
- **live 路径**：error-handling 全局错误中间件接进 server/index.ts；event-ledger 接 eventPolicy 过滤/脱敏；commands/compaction/executionContract 经核实**先前已接**；reply/security/provider-registry/message/executionApproval 经评估**会不可预测改变 live 行为或无干净插入点** → 留文档不接。
- **Group C（深子系统，需产品决策，全部保留不删）**：acp(24)/channels(15)/gateway(8)/agent 运行时(5)/cli(2)/孤立单例(~30) 共 ~54 文件。产出决策清单 `deliverables/backend-groupC-decision.md`（启用成本/建议/是否立项）。

### 验证
- 前端 tsc 全量 0 错误（exit 0）。
- 后端：新路由 + server/index.ts 挂载 `npx tsc -p server/tsconfig.json` 筛选**零错误**（全局 ~150 存量无关错误未动）。

### 关键经验
- "死"组件大多契约 100% 正确，**坏的是已发布 page 的 service/param**（Git 的 repoPath→path、Goals 的旧模型、Matching 的 MatchResponse 类型）。
- 并行代理必须**分区到不同文件**、共享文件由主代理集中改，否则并发编辑丢失/冲突。
- 向后端死模块接线时，先确认它是"替代实现/重复"还是"真缺路由"——前者留文档，后者加路由，避免双写冲突。

### 待办（用户决策）
- Group C 54 文件是否启用（见 deliverables/backend-groupC-decision.md）。
- 散件 15 项若确需消除 knip 误报，按文档逐项处理（多为重复实现，删其一更合适）。

## Group C（C1–C6）用户拍板「C1到C6都执行」（2026-07-11 下午）

用户指令："C1到C6都执行"。按**增量激活、绝不替换主执行链路(runChatSession/streamExecutor/chatService)** 安全边界执行，6 个并行代理各负责一簇，挂载点由主代理集中加进 server/index.ts。

### 接入结果（全部零删除）
- **C1 ACP**（24 文件）：`server/routes/acp.ts` → `POST /api/acp`(透传 handleAcpRequest，支持 sessions/list|get|create|close + health + tools/list + models/list) + `/api/acp/health` + `/api/acp/doctor`(runDoctorChecks)。turns/run 后端未实现→优雅报错 -32601。微调 doctor.ts 使其 runDoctorChecks() 签名合规。
- **C2 channels**（15 文件）：`server/routes/channelsCore.ts` → `/api/channels-core`（adapters 列表 / inbound 提交 / access 网关检查 / lookup），不动已挂载的 /api/channels。入站为内存队列+最小 stub，不触达 live 通道运行时。
- **C3 gateway**（8 文件）：`server/routes/gatewayExt.ts` → `/api/gateway-ext`（诊断 + mcpServer 独立状态，不拉起 stdio 以免劫持进程 I/O）；WebSocket Hub 依赖 httpServer 仅留集成片段，未强制挂。
- **C4 agent 运行时**（5 文件）：`server/routes/agentRuntime.ts` → `/api/agent-runtime`（subagent 编排桥接 + mcpManager 状态 + 健康快照）。`/subagents/run` 仅休眠原型桥接（返回 spawn 句柄，非真实执行），describe 端点暴露定义+工具集。
- **C5 CLI**（2 文件）：`bin/cdf-cli.mjs`(package.json bin) + `server/routes/cli.ts`(`POST /api/cli` 调 runCLI)；修复 program.ts 参数 bug（runCLI 原把已剥离参数又裸喂 commander 致所有子命令只回 --help）。
- **C6 单例**（~30 文件）：`server/routes/capabilities.ts` → `/api/capabilities`（17 个只读探测端点：thinking-modes/tool-search/fast-mode/model-metadata/tool-policy/few-shot-templates/session-fingerprint/envelope/config/infra-retry|dedupe|file-lock/errors-failover/streaming/hooks/context-cache/heartbeat）+ `logging/redact` 接入 server/index.ts 全局错误中间件脱敏。其余（toolSearch/hooksManager/contextCache/streamingHandler/toolPolicy/thinkingMode/fastMode/modelMetadata/fewShotTemplates 等）因会改变 LIVE 行为仅以只读 HTTP 面暴露，未强接热路径；codeUnderstanding/workspaceManager/web-content-extractors/nodeManager/authProfilePool/deviceAuth/channel*/apiRegistry/oauthTypes/embeddedRuntime 等低确定性项 DOCUMENT 接线点保留。

### 验证
- 后端 `npx tsc -p server/tsconfig.json` 全量 **0 错误**（含 server/index.ts 挂载）；C1–C6 全部新增/改动文件 grep 零错误。
- 安全边界守住：runChatSession/streamExecutor/chatService 行为未变。

### 交付物
- `deliverables/backend-groupC-decision.md` 状态表已更新为「✅ 已增量接入」+ 安全边界说明。

## 续作：C1–C6 验证 + 真正挂载 + live 接线（用户"三个都做"）

用户确认执行上轮末尾给的 3 个可选方向：① 新路由冒烟测试 ② WebSocket Hub 挂 httpServer ③ codeUnderstanding 接 live。

### ① 新路由冒烟测试（已加，31 用例全过）
- `e2e/api/groupC-smoke.test.ts`：ACP/channels-core/gateway-ext/agent-runtime/capabilities/cli 共 23 用例（supertest + 动态 import 隔离）。
- `e2e/api/gateway-ws.test.ts`：真实 httpServer + 挂载 WS Hub + 客户端连 `/gateway/ws` 收 `connected` 事件（端到端验证）。
- `e2e/api/code-understanding.test.ts`：HTTP 面 4 端点 + handler + `initDefaultTools` 注册断言，7 用例。
- **合并运行 31/31 通过。**
- 修复：`/api/acp/doctor` 原因 `getGlobalChannelRegistry` 未注入抛 500；改 `doctor.ts` 的 `checkChannels` 防御性降级（未注入时返回 info，行为不变）。

### ② C3 WebSocket Hub 真正挂载 httpServer
- `server/index.ts` 的 `server.listen` 回调内新增动态 `import('./gateway/webSocketHub.js').then(startGatewayWebSocket)`，非阻塞、失败仅 warn。
- 路径 `/gateway/ws`，双向实时通道，绝不劫持/替换 SSE。`ws` 包已安装。

### ③ C6 codeUnderstanding 接 LIVE 工具解析链路
- 新增 `server/engine/codeUnderstandingTool.ts`（`handleCodeUnderstanding`，按 action 分发 analyzeFile/analyzeProject/explainSymbol/suggestImprovements，返回 JSON 串）。
- `server/engine/toolRegistry.ts` 的 `initDefaultTools` 内动态注册内置工具 `code_understanding`（纯增量，不替换现有工具）。
- 新增 `server/routes/codeUnderstanding.ts` → `/api/code-understanding`（只读分析面），挂载于 `server/index.ts`。

### 验证
- 后端 `npx tsc -p server/tsconfig.json` 全量 **0 错误**（本轮前 ~150 存量无关错误现已清零，整个 server 干净编译）。
- `npx vitest run` 相关用例 **31/31 通过**。
- 安全边界守住：runChatSession/streamExecutor/chatService 行为未变；code_understanding 为新增工具，仅扩展能力。

### 关键经验
- 死代码簇接入后的权威验证 = 冒烟测试（supertest 直接挂 router）+ 后端 tsc grep 过滤 + 最终全量 tsc。**测试能暴露真实缺陷**（如 doctor 的 500），不要只看 tsc 零错误。
- Express 5 无 `app.router`，测试里 `app.use(app.router)` 会让所有请求 500；应保持 `express.json()` + `app.use(router)` 的最小挂载。
- 增量接入深子系统时，WS Hub 这类依赖 httpServer 的，用 `server.listen` 回调内动态 import 挂载最干净，避免与主链路耦合。

## OpenClaw ↔ cdf-know-clow 完整度分析（用户"分析 openclaw 和现在的软件"）

产出 `deliverables/openclaw-vs-cdfklow-完整度分析.md`。

### 核心结论
- **cdf-know-clow 是 OpenClaw v2026.6.9 的硬 fork**（FORK_BOUNDARY.md 证实）：`openclaw/` 是被 `.gitignore` 忽略的 vendored 参考副本，**运行时完全不读它**；真正运行的是 `packages/` 下 `@cdf-know/*`（5 包：plugin-sdk 21/agent-core 20/llm-core 8/memory-host-sdk 12/skill-core 13）。
- 整体 fork 完整度 ≈ 35–40%：引擎核心层 ≈60%（多处超规格自研），生态广度层 ≈15%（25通道/52 Provider/40工具/原生端/Canvas/语音/媒体刻意未跟随），WMS 域 + 桌面壳(电子/PyWebView) 为独有 100%。
- 桌面壳实测：**Electron 入口(electron/main.ts) + PyWebView(pywebview_app，仅 .pyc 残留) 并存**，与 OpenClaw 原生 android/ios/macos/windows 路线不同。

### 本次整合（死代码全做）后完整度增量
agent 管理与网关扩展面"可读/可探"完整度 ~5%→~25%；但 ACP turns/run、通道入站、subagents 真实 spawn 仍 0（安全边界必然结果）。

### 下一步方向（报告 P0–P3）
- P0：收口 ReAct v6 三大缺口（A reflection 关闭确认是否 intentional / B planner replan / C 前端 T04 5 事件消费）。
- P1：建立 fork 同步纪律（openclaw/ 改 pinned submodule 或 SHA vendored + drift CI；修正 4 个上游修复未达运行时问题）。
- P2：按 WMS 桌面场景选择性吸收 OpenClaw 广度（Provider/MCP 工具/多智能体高优；通道/Canvas/语音/媒体跳过）。
- P3：工程成熟度对齐（knip 死代码 CI 闸门、maturity-scores 量化基线、扩展 e2e 回归）。

## P2-1b 智能技能路由·自动注入（openclaw discovery+research × advanced-triggers）

延续 P0-A（技能数据链路）+ P2-1a（匹配引擎聚合 folder-skill）的「智能技能路由」第二层：把自动匹配结果注入 system prompt。

### 新增模块 `server/engine/skillRouter.ts`
- `routeSkillsForPrompt(query, contextMessages, {topK,threshold})`：自动匹配 top-N 相关技能。
- `formatRoutedSkillsForPrompt(skills)`：格式化为 `<available_skills>` XML 块，调用指令指向 P0-A 已落地的 `skill use <id>` 元工具（非无 handler 的 skill_<id>）。
- `extractContextTexts(dbMessages, limit)`：取最近若干条 user/assistant 文本作路由上下文。
- `resolveSkillContext(upstream, query, ctx)`：合并上游（前端/队列显式）skillContext + 自动路由块，任一环节失败仅回退不阻断。
- `buildSkillLookup()`：覆盖 builtin(@src/types/skill-core) + 用户自建(dao/skills) + folder-skill(getFolderSkillsForMatching) 三来源，与 matchingService.collectAllSkills 一致，避免命中技能被丢弃。

### 关键修复：避免请求期 ONNX 模型下载阻塞
- `match` 的 context/hybrid/semantic 模式会触发 `semanticSearch` → `initOnnxEmbedding()` 从 HuggingFace 下载 all-MiniLM-L6-v2（离线约 30s×3 重试 ≈ 90s 阻塞）。
- 改为：**永远先跑 keyword 模式（瞬时、已验证命中 notion/trello/github）**；仅当 `getOnnxStatus().status==='ready'`（本地模型已就绪，同步调用不触发下载）时才额外叠加 context 语义增强。离线/未预下载环境绝不触发下载。

### 接线（两条执行路径都接，不影响主链路行为）
- `server/routes/chatService.ts` 的 `executeQueuedMessage`（队列模式，UI 默认）：`resolveSkillContext(params.skillContext, params.message, extractContextTexts(dbMessages,6))` → 传入 `buildApiMessages.skillContext`（step4 注入 system message）。
- `server/engine/runChatSession.ts`（直接模式）：同模式接线。

### 验证
- tsc 针对性筛选 skillRouter/chatService/runChatSession **零错误**。
- 冒烟（离线、keyword 模式、瞬时）：`notion`→notion(0.5)、`github issue`→github(0.5)、`coding-agent`→coding-agent(0.4) 命中；`summarize/diagram/himalaya` 关键词未命中（需语义，离线按预期降级，生产模型就绪后 context 补召回）。`resolveSkillContext` 正确合并上游+路由块。

### 待办
- P2-1c：修正 `skillChatCommands.formatSkillsForPrompt`（:545）及技能调用指令，统一指向 `skill use <id>` 元工具。
- P2 lifecycle / security 增强未做。
- 仓库仍有一批并行未提交改动的预存 tsc 错误（他人 v11.0 工具调用安全审查不完整功能 toolExecutor/toolCallReviewer），非本会话引入。
