# 2026-07-15 工作日志

## 多 Agent 架构 v8.0 — 前端收尾 + tsc 零错误

### 修复内容

1. **`src/hooks/useChat.ts`**
   - `SendMessageOptions.executionMode` 联合类型补全 `'orchestrator'`（之前遗漏导致 TopBarChatInput.tsx 第 509 行 TS2322 报错）

2. **`src/hooks/useChat.ts` — SSE 编排事件处理**
   - 新增 `task_decomposition`：初始化 `streamingMsg.orchestrationState`
   - 新增 `sub_task_start`：标记子任务状态为 `running`
   - 新增 `sub_task_progress`：更新子任务进度条 + 工具调用日志
   - 新增 `orchestration_complete`：填充 `stats`，标记所有未完成子任务为 `completed`

3. **`src/components/CrossWmsChat/ChatMessageList.tsx`**
   - MUI import 新增 `LinearProgress, Chip, Accordion, AccordionSummary, AccordionDetails`
   - 新增 `OrchestrationCard` 组件：展开/折叠子任务列表、状态 Chip 颜色、进度条、工具调用日志

4. **`src/components/Layout/AISettingsDialog.tsx`**（`orchestrator` 选项之前已加入，本次确认）

### 编译结果

```
npx tsc --noEmit  →  0 errors
```

### 遗留任务（全部已完成 ✅）

- ~~Task #18: MCP tool call flow~~ — 已完成
- ~~Task #29: 上下文溢出自动压缩~~ — 已完成
- ~~Task #30: 模型不支持自动降级~~ — 已完成

---

## 上下文溢出自动压缩 — 集成完成

### 改动

1. **`server/engine/contextCompress.ts`**
   - `compressContextWithSummary()` 新增 `workingMemoryMessages` 参数（与 `truncateContextForModel` 一致）

2. **`server/engine/toolExecutor.ts`**
   - `import { compressContextWithSummary }` + 替换 `truncateContextForModel()` → `compressContextWithSummary()`

3. **`server/engine/executionStrategy.ts`**（ObserverStrategy）
   - 同上替换，`await compressContextWithSummary(currentMessages, ctxWindow, ctxMaxTokens, tools.length, modelConfig)`

4. **`server/engine/reactExecutor.ts`**
   - 同上替换，传入 `workingMemoryMessages` 参数

### 效果

上下文溢出时先 LLM 摘要（保留关键信息），再截断。摘要失败自动降级为简单截断。

---

## 模型不支持自动降级 — 补全完成

### 改动

1. **`server/routes/chat.ts` — `executeFromQueue` 降级逻辑**
   - 新增 `AIAPIError` 识别 + 降级链（同 provider 非推理 → 任意可用）
   - 降级成功：发送 `done` 事件含 `fallbackModel` + `fallbackReason`
   - 降级失败：回退到原错误处理

2. **`src/types/chat.ts`**
   - `Message` 新增 `fallbackModel?: string` + `fallbackReason?: 'model_not_supported' | 'request_failed'`

3. **`src/hooks/useChat.ts`**
   - `done` 事件提取 `data.fallbackModel` → `streamingMsg.fallbackModel`

4. **`src/components/CrossWmsChat/ChatMessageList.tsx`**
   - 消息头部模型名旁显示 `⚠️ 已降级到 xxx` Chip

### 编译结果

```
npx tsc --noEmit  →  0 errors
```
