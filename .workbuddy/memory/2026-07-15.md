# 2026-07-15 工作日志

## CrossWMS Debug 专项分析（排障手 / gstack-investigator 框架）
- 单成员直调：用 Explore 承载排障手框架，对 CrossWMS debug 子系统做专项分析（背景资料称 PyWebView，实为本仓 CDF Know Clow：Swift/WKWebView + Express）。
- 严重度分布：🟢 3 / 🟡 6 / 🔴 1（共 10 条）。
- 关键发现：
  - 🔴 SSE 双实现漂移：sseHelper.ts:42-57 messageQueue 裸 res.write 发 queue_event，绕过 sendSSE/sendDebugSSE，无 LOG_DEBUG 门控、类型不在 ANY_EVENT_TYPES（sseTypes.ts:512）。
  - 🟡 channels/outbound 裸 console.*：retry-queue.ts:268/299、lifecycle-manager.ts:135/260/271、pipeline.ts:147，生产无 stdout 时错误静默丢失。
  - 🟡 debug 载荷泄露：chatService.ts:256-266 onSSEEvent 把未知 type（含 tool_call toolArgs/result）经 sendDebugSSE 转发未脱敏，LOG_DEBUG=1 即推前端。
  - 🟡 sseHelper.sendErrorEvent(91-103) 发 type:'done' 而非核心 type:'error'，潜在 bug/死代码。
  - 🟡 debug 事件缺 sessionId/requestId；logger.error 未携带 classifyAndFormatError 的结构化 code。
  - 🟢 LOG_DEBUG 开关一致（sseTypes.ts:258-268 + logger.ts:23-24）；主错误路径 runChatSession.ts:894-945 正确发 error+done。
- 优先修复：queue_event 改经 sendSSE+LOG_DEBUG 门控；channels/outbound 裸 console 换 logger；sendDebugSSE 入口脱敏；注入 sessionId/requestId；修复/删除 sendErrorEvent + 明确双 SSE 取舍。
- 未落盘（单成员直调默认对话输出）；用户未要求正式报告。

## Debug 专项修复执行（主理人降级直调）
- 探索 agent（Explore）无法写文件（Edit not found），gstack 原生子 agent 未注册、general-purpose 在 team 崩溃，故主理人直接执行修复（以排障手框架指导）。
- 修复 3 项（聚焦明确 bug，未动架构取舍项 sendErrorEvent/双 SSE 合并/sessionId 注入）：
  1. server/routes/chatHelpers/sseHelper.ts：messageQueue.on('queue') 裸 res.write 发 queue_event → 改调 sendDebugSSE（统一通道 + LOG_DEBUG=1 门控），新增 import。
  2. server/sse/sseTypes.ts：sendDebugSSE 入口新增 sanitizeForDebug() 脱敏（敏感字段名整字段 [REDACTED]、绝对路径打码 <path>、超长字段截断 200 字符），防密钥/Token/路径经 debug 通道泄露前端。
  3. server/channels/outbound/{retry-queue.ts, lifecycle-manager.ts, pipeline.ts}：6 处裸 console.error/warn → logger（带 [retry-queue]/[lifecycle]/[pipeline] 标签）。
- 验证：tsc --noEmit -p server/tsconfig.json 零错误 ✅；vitest sseStreaming.test.ts 18/18 ✅；vitest server/ 全量 2619 passed / 16 failed / 43 skipped。
- git stash 对比：干净 main 同样 16 failed（2647 passed），证明 16 失败为项目预存在，本次修复未引入回归 ✅。
- IS_PASS：通过（本次 debug 修复无回归）；全量 16 失败非本次引入（gateway qwen 规范化、toolRegistry embedBatch 原生模块超时等），建议另立任务处理。
- 改动未提交、未落盘报告。
