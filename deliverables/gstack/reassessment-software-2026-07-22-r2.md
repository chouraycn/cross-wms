# CrossWMS 软件现状重新评估 · 第 4 轮（2026-07-22 晚间）

**基线对照**：`reassessment-software-2026-07-22.md`（13:52，🔴 No-Go，1894 类型错误）

---

## 📌 TL;DR

- **判定：🔴 No-Go 维持**，但**失败模式已改变**——从"类型破产"转为"门禁虚设 + 安全架构阻塞"。
- **最大变化**：server tsc 从 **1894 错误 → 0 错误**、frontend tsc **0 错误**，测试 **46/46 绿**（工作树内，全部未提交）。类型债已在树内还清。
- **真正根因（新发现）**：类型门禁在配置里存在，但**实践中零 enforcement**——`.git/hooks/pre-commit` 为空（husky 未装进 git）、提交直推 main（绕过 PR 门禁）。1894 错误能进主干的唯一原因。
- **剩余 No-Go 驱动项**：4 个安全/架构 P0（chat 鉴权、0.0.0.0 绑定、双层存储、跨境能力）+ 回滚预案 + 359 文件未提交工作树。

---

## 1. 当前真实状态（20:33 实测）

| 维度 | 结果 | 说明 |
|------|------|------|
| server tsc（`-p server/tsconfig.json --noEmit`） | 🟢 **0 错误** | 工作树内，未提交 |
| frontend tsc（`--noEmit`） | 🟢 **0 错误** | 工作树内，未提交 |
| 核心测试（sseStreaming 18 + chatService 28） | 🟢 **46/46 绿** | 冒烟实测通过 |
| Git 总变更 | 🟡 **359 文件**（157 M + 202 ??） | 版本 1.7.129 |
| HEAD | 🔴 `2ba64c2`（13:52 报告时尚处 1894 错误态） | 工作树修复未提交 |

---

## 2. 根因分析：门禁为何失效（本轮最关键发现）

| 门禁层 | 配置现状 | 实际生效？ | 证据 |
|--------|---------|-----------|------|
| pre-commit 钩子 | `.husky/pre-commit` 写了双 tsc 检查 | ❌ **未生效** | `.git/hooks/pre-commit` 为空；`package.json` 有 `prepare: husky` 但未真正安装 |
| PR 质量门禁 | `pr-quality-gate.yml:135` 跑 `tsc -p server` | ⚠️ 仅 PR 触发 | 提交**直推 main**，无 PR merge commit，门禁被绕过 |
| 分支保护 | — | ❌ 无 | main 可被直接 push |

**结论**：1894 错误进主干不是"漏跑一次"，而是**没有任何强制关卡在 main 上**。只要直推 + 不用 `--no-verify` 之外的任何约束，类型债必然反复累积。这是必须先用最低成本堵死的**系统级漏洞**。

---

## 3. 阻塞项重评（对照 13:52 报告）

| # | 阻塞项 | 13:52 | 本轮 | 变化 |
|---|--------|-------|------|------|
| 类型健康（server） | 🔴 1894 | 🟢 0（树内） | **已还清（未提交）** |
| 测试完整性 | 🟢 46/46 | 🟢 46/46 | 维持 |
| **门禁 enforcement** | — | 🔴 **0 生效** | **新增·根因** |
| #3 chat 路由鉴权 | 🔴 | 🔴 `routes/chat.ts:23` 无 middleware | 未解 |
| #4 HTTP 绑定 0.0.0.0 | 🔴 | 🔴 `index.ts:635` + `ports.ts:32` 显式 0.0.0.0 | 未解 |
| #5 路径穿越 | 🟡 | 🟢 `security-path.ts` 已接入 | 已缓解 |
| #6 双层存储 | 🔴 | 🔴 无对应实现 | 未解 |
| #7 跨境能力 | 🔴 | 🔴 仅路由层，无真实报关/支付 | 未解 |
| #8 回滚预案 | 🔴 | 🔴 无 runbook | 未解 |
| #9 WKWebView E2E | 🔴 | 🔴 未解 | 未解 |
| #10 ChatThread @keyframes | 🔴 | 🔴 未解 | 未解 |

> 净变化：1 项已缓解（#5），类型项由红转绿（树内）。**但新增"门禁虚设"根因项，且 6 项安全/架构 P0 仍为硬阻塞。**

---

## 4. 下一步方向（按优先级，可直接执行）

### 🔴 P0-A：让门禁真正生效（根因，30 分钟内可完成）
1. **安装 husky 钩子**：`npx husky install`（或重跑 `npm install` 触发 `prepare`）→ 确认 `.git/hooks/pre-commit` 非空且能在故意制造错误时阻断提交。
2. **main 分支保护**：在 GitHub 开启 "Require a pull request before merging" + Require status check `pr-quality-gate`（已含 server tsc）；禁止直推。
3. **一次性还贷**：当前 359 文件工作树（类型已绿）通过**合规 PR** 合入，由门禁验证而非 `--no-verify`。

### 🔴 P0-B：安全/架构硬阻塞（发布前必须，顺序即优先级）
1. **#4 host 绑定**：`server.listen(PORT)` → `server.listen(PORT, HOST)`，`HOST` 默认 `127.0.0.1`、可配置。改动 ~1 行 + config。最快见效。
2. **#3 chat 鉴权**：为 `/chat`、`/agent-chat`、`/api/gateway/chat/*` 增加 `requireAuth` middleware（参考 `automation.ts` 的 `authenticateWebhook` 模式）。
3. **#6 双层存储**：产品决策——SQLite 与 JSON 文件二选一或明确分层，消除数据一致性风险。
4. **#7 跨境能力**：产品范围决策——是否纳入报关/多通道支付，决定 MVP 边界。
5. **#8 回滚预案**：出 release runbook（DMG 回滚、配置回滚、数据迁移回退）。

### 🟡 P0-C：提交卫生（避免再次堆积巨型 diff）
- 将 359 文件**按关注点拆分 PR**：① 类型还贷 + 门禁；② skills 可观测性（132 engine + 5 前端页面）；③ adapters/channels 新增（20+12 文件）；④ 其余。每个 PR 独立过门禁。

### 🟢 P0-D：重新基线
- 完成 P0-A + P0-B 的 #3/#4 后，出**第 5 轮评估**：若门禁生效 + #3/#4 修复，判定可由 🔴 降为 🟡（条件 Go，待 #6/#7/#8 产品决策）；#9/#10 可并行排期。

---

## 5. 一句话结论

> 类型破产已在工作树内自愈，但**问题是"没人拦得住坏提交"**。下一步不是继续写功能，而是先用最低成本把**门禁焊死在 main 上**，再逐个清掉 4 个安全/架构 P0；否则每轮"重新评估"都只会重复今天的剧情。

---

*生成方式：本轮回合为单主理人实测复析（非 gstack 子 agent）。所有数值为 20:33 本地实测。*
