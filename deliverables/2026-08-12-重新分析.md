# CrossWMS / CDFKnow 重新分析（2026-08-12）

> 承接《2026-08-10 整体分析与下一步开发方向》（停于 `main` / `510b2f07` / ~v1.7.194）。
> 本报核心变化：**API 信封迁移 6 批全量收口 + 契约测试落地**、**统一弹窗层级管理**、**WKWebView confirm 对话框迁移（未提交）**、**UI 视觉统一（未提交）**。
> 所有数字基于 2026-08-12 21:19 实测：git status / git log / tsc / wc -l。

---

## 0. 一句话结论

**P2-1 API 契约对齐——这个拖了多轮分析的唯一"卡在人拍板"阻塞项——已彻底收口。** 6 批 / 25 文件 / 251 调用全量迁移 + 契约测试落地 + skip 规则文档化。当前主线转向**WKWebView 兼容性修复（window.confirm → MUI Dialog）+ UI 视觉统一**，但这两块改动尚未提交，且本地与 origin 已分叉（差 1 commit）。下一步最该做的是：**提交在飞的 23 处改动 → 解决 origin 分叉 → 推进 `refactor/staff-dedup-mcp` 合入 main（已拖两轮分析）**。

---

## 1. 当前状态快照（对比 08-10）

| 维度 | 08-10 报 | 本报（08-12） | 变化 |
|---|---|---|---|
| HEAD / 版本 | `510b2f07` / ~v1.7.194 | **`0c755b17` / v1.7.213** | +19 版本，16 commits |
| 当前分支 | `main` | `main` | 同 |
| 脏树 | 10 处 | **52 处（23 tracked + 29 untracked）** | ↑42，大量在飞改动 |
| .git 体积 | 109M（08-09 测） | **116M** | +7M，新 commit 增量 |
| tsc --noEmit | EXIT=0（CI 门禁已 inline） | **EXIT=0 / 0 error** | 稳 ✅ |
| 前端 src 行数 | 177,019（08-09 测） | **179,450** | +2,431 行 |
| engine 行数 | 272.3 万（08-09 测） | **272.4 万** | +876 行，基本持平 |
| components/staff 引用 | 21 文件 | **22 文件** | +1 |
| API 信封迁移 | 基建就位，卡在拍板 | **6 批完成，25 文件 / 251 调用，契约测试落地** | ✅ 收口 |
| origin 同步 | 未提及 | **本地 behind 1（`2a859bb6` v1.7.212 bump）** | ⚠️ 分叉 |
| refactor/staff-dedup-mcp | 待真机 e2e 后合 main | **仍未合入（3 commits not in main）** | ❌ 拖两轮 |

---

## 2. 08-10 后关键提交（16 commits，v1.7.194 → v1.7.213）

| Commit | 类型 | 内容 |  significance |
|---|---|---|---|
| `f9660a08` | refactor | API 信封迁移首批 — 9 个 AUTO 安全路由(138 处) | 最大单批 |
| `94e8c4e1` | fix(dmg) | 沙箱快速模式注入 DS_Store + 背景图 + 卷图标 | 打包修复 |
| `7cd4bc54` | refactor | 信封第二批 — 6 文件(20 处) | |
| `d259973a` | refactor | 信封第三批 — capabilities/toolMonitoring(32 处) | |
| `c24f0801` | refactor | 信封第四批 — wikiService/workflow(23 处) | |
| `fe9d1842` | refactor | 信封第五批 — skillWorkshop/keywordTrigger(23 处) | |
| `08aee5a5` | refactor | 信封第六批 — insights/audit/apiHistory/apiDomainWhitelist(15 处) | 收尾批 |
| `3487568d` | chore | 收口：routeMetrics/staff 路由/errorHandler/UI/**契约测试**/deliverables/skills | 综合收口 |
| `4bf02cf6` | fix | 补回 rebase 丢失的 SettingsPopover 嵌套菜单改动 | rebase 坑修复 |
| `8f8840e1` | fix | 设置弹窗分组从下拉展开改为子视图切换 | UX 改进 |
| `0c755b17` | feat | **统一弹窗层级管理（L0-L6 递增 z-index + ApprovalDialog Portal 化）** | 弹窗架构升级 |

**两大主题**：
1. **API 信封迁移全量收口**（6 批 + 收口 + 契约测试）
2. **弹窗/设置 UX 升级**（z-index 层级体系 + 子视图切换 + rebase 修复）

---

## 3. API 信封迁移：最终状态

### 3.1 数字总览

| 指标 | 值 |
|---|---|
| 已迁移文件 | **25 文件 / 251 调用**（6 批） |
| 使用 `respond.ts` 的路由文件 | **48 文件** |
| 仍有 `res.json` 的路由文件 | **90 文件 / 616 调用** |
| 契约测试 | `tests/contracts/p21-envelope.contract.test.ts` ✅ |
| 迁移技能 | `api-envelope-migration` skill 已建于 `~/.workbuddy/skills/` ✅ |

### 3.2 剩余 90 文件分类（全部确认跳过，非裸返回）

| 类别 | 代表文件 | 跳过原因 |
|---|---|---|
| **A. 已合规** `{code:0,data,message}` | transfer, wms-alert/inventory/replenishment/quality/outbound | 已是信封格式 |
| **B. 全手动信封** `{ok:true}`/`{success:true}` | cache, codeIndex, pdf, lsp, tts, stt, webhook, mediaLibrary, video/music/image-generation, pluginSdk | 自有信封体系 |
| **C. raw 消费者 / 平台契约** | browser, mcp, soul, contextEngine, process, cron, nodeHost, pairing, plugins, git, memory, taskMonitor, secretsService, goalsService, channel-webhook, agentChat, apiTemplates | 前端 raw fetch 直读顶层键 / 第三方平台契约 |
| **D. 协议契约** | acp, codeUnderstanding | 自带 envelope |
| **E. 双包敏感（DEFER）** | automation(14), trigger(14) | 已是 `{data,total}` payload，消费者 `result.data` 依赖此结构，需前后端协同重构 |
| **F. 剥包中间件** | staff/* (14 文件 ~130 调用) | `server/index.ts:394-419` 剥包中间件保留 envelope |

**结论**：可安全迁移的真裸返回已挖尽。DEFER 池仅 automation/trigger 需协同重构（风险 > 收益，维持现状）。**P2-1 在既定范围内已彻底完成。**

### 3.3 Top res.json 文件（均在跳过类）

```
34  contextEngine.ts     (C - raw consumer)
33  pdf.ts                (B - manual envelope)
29  browser.ts            (C - platform contract)
26  lsp.ts                (B - manual envelope)
19  staff/agents.ts       (F - strip middleware)
17  staff/skills.ts       (F)
17  staff/knowledge.ts    (F)
16  codeIndex.ts          (B)
15  staff/chatStream.ts   (F)
14  trigger.ts            (E - DEFER)
14  automation.ts         (E - DEFER)
```

---

## 4. 在飞改动（23 tracked modified，未提交）

### 4.1 WKWebView confirm 对话框迁移（~15 页面）

**问题**：WKWebView 环境下 `window.confirm()` / `window.prompt()` 不弹窗，导致删除/危险操作无确认拦截。

**方案**：新建 `src/utils/confirmDialog.tsx`，用 MUI Dialog + `createRoot` 实现异步 `showConfirm()` / `showPrompt()`，替代 `window.confirm`。

**已改页面**（均为 `window.confirm` → `await showConfirm`）：
- ApiKeysPage, AutomationPage, ChannelsPage, ExtensionsCenterPage, ExtensionsPage
- MediaLibraryPage, PairingPage, ProcessManagementPage, SecretsPage, SkillsPage
- WorkflowPage, CronPanel, BrowserControlPanel, KeywordTriggerStatsPanel, ToolManagementDialog

**进度**：剩余 4 文件仍有 `window.confirm` 引用（含 confirmDialog.tsx 自身注释），迁移接近完成但**尚未提交**。

### 4.2 UI 视觉统一

| 文件 | 改动 | 方向 |
|---|---|---|
| `CenterPage.tsx` | 移除 title/description 头部块，简化 padding/overflow | 扁平化，对齐 WikiPanel 风格 |
| `ExtensionsPage.tsx` | 297 行重写：去 Card/Paper/Grid，改 Box+border+IconButton | 视觉对齐 WikiPanel |
| `SkillsPage.tsx` | 删除确认改为 MUI Dialog state（`deleteConfirmSkill`/`deletingSkill`） | 替代 window.confirm 的增强版 |

### 4.3 脚本/打包改进

| 文件 | 改动 |
|---|---|
| `scripts/package-mac-dist.sh` | tag 检查增强：同时检查 local + remote tag，修复 re-tag/re-release 场景 |
| `scripts/verify.mjs` | 整体重写：从 CLI check+test runner → Playwright 浏览器验证脚本 |

### 4.4 版本文件

- `package.json` / `release/release.json`：本地版本与 HEAD 不一致（疑似本地发布残留）

---

## 5. 整体成熟度评估（四象限，对比 08-10）

| 象限 | 08-10 评级 | 本报评级 | 变化判断 |
|---|---|---|---|
| **架构** | 🟡 中等偏稳 | 🟡 中等偏稳 | 无质变。弹窗 z-index 层级体系(L0-L6)是正面增量，但 engine 272 万行 OpenClaw 硬分叉未变。 |
| **能力** | 🟢 较完整 | 🟢 较完整 | window.confirm 迁移补齐 WKWebView 兼容性短板；verify.mjs 改为 Playwright 浏览器验证。 |
| **质量 / 工程** | 🟡 关键债未清 | **🟢→🟡 好转** | **P2-1 API 信封收口**是最大增量——从"卡在人拍板"变为"6 批落地 + 契约测试 + skip 规则文档化"。但 engine 测试门禁仍未固化、23 处在飞未提交、origin 分叉。 |
| **产品 / 方向** | 🟢 主线清晰 | 🟢 主线清晰 | UI 视觉统一 + WKWebView 兼容修复印证"桌面端体验打磨"方向。 |

**总评**：工程质量债从"两块短板"变"一块半"——P2-1 已清，engine 测试门禁仍悬空。整体从"能跑"向"可交付"的门槛又过了一步。

---

## 6. 关键风险与阻塞

| # | 风险 | 等级 | 状态 | 与 08-10 对比 |
|---|---|---|---|---|
| 1 | **origin 分叉**（local behind 1, `2a859bb6` v1.7.212 bump 未拉取） | 🟡 中 | 新增 | 08-10 未提及；需 `git pull --rebase` 解决，注意铁律：rebase 后 `git show HEAD` 验证关键改动 |
| 2 | **23 处在飞改动未提交** | 🟡 中 | 恶化 | 08-10 为 10 处 → 23 处；window.confirm 迁移 + UI 统一 + 脚本改进堆积，应尽早提交 |
| 3 | **`refactor/staff-dedup-mcp` 未合入 main** | 🟡 中 | 未变 | 已拖两轮分析；3 commits not in main（StaffDeck submodule fix + vitest timeout + DMG decouple） |
| 4 | **engine 测试门禁未固化** | 🟡 中 | 未变 | `vitest.config.ts` 仍有 15 处 `server/engine` 引用；851 unresolved imports 阻塞 |
| 5 | **上游同步悬空** | 🟠 高 | 未变 | `sync/openclaw-2026-08-04` 自 08-04 未动，401 文件待审阅 |
| 6 | **automation/trigger 协同重构** | 🟢 低 | DEFER | 需前后端同步改消费者，风险 > 收益，维持现状 |

---

## 7. 下一步开发方向（优先级路线图）

### P0 — 收口与同步（立即）

| 动作 | 退出标准 |
|---|---|
| **提交 23 处在飞改动**：先 `tsc --noEmit` EXIT=0 → 按铁律 `git commit --no-verify`（精确列文件名） | 脏树归零（仅 untracked 诊断脚本） |
| **解决 origin 分叉**：`git pull --rebase` → `git show HEAD:<关键文件>` 验证 → push | local 与 origin/main 完全同步 |
| **合并 `refactor/staff-dedup-mcp`**：3 commits（StaffDeck submodule fix + vitest CI + DMG decouple）合入 main | main 含 CI 修复；分支拓扑清理 |

### P1 — 工程质量硬化（本迭代）

| 动作 | 退出标准 |
|---|---|
| **window.confirm 迁移收尾**：剩余 4 文件完成迁移，全仓 `grep window.confirm` 仅剩 confirmDialog.tsx 注释 | WKWebView 下所有确认对话框正常弹出 |
| **UI 视觉统一推进**：以 ExtensionsPage/CenterPage 为样板，统一剩余页面的 Card→Box+border 风格 | 主要页面视觉一致 |
| **固化 engine 测试门禁**：CI 流程改为 `build openclaw` → `test:engine` → 主套件移除 `server/engine/**` | engine 覆盖率可见；主套件 0 解析失败 |
| **verify.mjs 新版验证**：Playwright 浏览器验证脚本接入 CI | 替代旧 CLI check+test 流程 |

### P2 — 上游与产品深化（下迭代起）

| 动作 | 退出标准 |
|---|---|
| **建立上游同步长效机制**：定频把 `sync/openclaw-*` 401 文件做差异评审 | 同步 runbook + 首次增量合入 |
| **数字员工能力闭环**：沉淀"员工 ↔ 技能 ↔ 工具"注册/发现/权限标准 | 可演示：新员工接入 ≤ 半天 |
| **automation/trigger 协同重构**（可选）：改 `automation/api.ts` 消费者 + 后端统一 envelope | 双包问题消除 |

### 可暂缓（维持现状）
- 去 iframe 方案：iframe 当前是唯一稳定实现，维持。
- 回退 engine 为 submodule：33% 独有代码，不回退。

---

## 8. 建议的"单一最重要下一步"

> **立即提交 23 处在飞改动并解决 origin 分叉。**
>
> 理由：在飞改动已从 08-10 的 10 处膨胀到 23 处，涵盖 window.confirm 迁移、UI 视觉统一、脚本改进三条线，堆积越久与 main 的对话框重构越易冲突。同时 local 与 origin 已分叉（差 1 commit），`git pull --rebase` 必须尽快执行——按铁律 rebase 后 `git show HEAD` 验证关键改动未被静默丢弃。**先收口再推进**，避免在分叉基础上继续累积改动导致 rebase 成本指数增长。

---

## 9. P0 执行结果（2026-08-12 21:45 追加）

用户"都执行"后，P0 三项已全部处理：

### 9.1 ✅ 在飞改动已提交（5 commits，push 至 origin/main）

| Commit | 内容 | 文件数 |
|---|---|---|
| `ef57086f` | feat: WKWebView confirm/prompt 迁移（showConfirm/showPrompt） | 15 |
| `de82a356` | style: UI 视觉统一（CenterPage/ExtensionsCenterPage/ExtensionsPage） | 3 |
| `33762a79` | chore: package-mac-dist 远程 tag 检查 + verify.mjs 重写为 Playwright | 2 |
| `d49e0118` | chore: bump v1.7.213（package.json + release.json + MEMORY.md） | 3 |
| `5a5d4464` | docs: 2026-08-12 重新分析报告 | 1 |

- 提交前 tsc EXIT=0；按铁律 `--no-verify` + 精确列文件名。
- 排除项未提交：e2e 测试快照（恢复）、诊断脚本/截图（untracked 保留）。

### 9.2 ✅ origin 分叉已解决

- `git pull --rebase` 时 package.json 冲突（CI bot 的 `2a859bb6` 提到 1.7.212 vs 本地 1.7.213），解决为 1.7.213。
- **坑**：`rebase --continue` 反复 exit 137——根因是**孤儿 Vite dev server 双实例吃光内存**（pages free 仅 ~57MB）+ rebase 机制异常。清理进程后改用「直接 `git commit -F .git/rebase-merge/message` + `cherry-pick` + `git rebase --quit` + `git branch -f main HEAD`」四步完成，规避死循环。
- rebase 后按铁律 `git show HEAD` 验证：package.json=1.7.213、release.json=1.7.213、confirmDialog 存在、SettingsPopover 子视图切换完好、MEMORY.md 铁律条目在。
- push 成功：`2a859bb6..5a5d4464`，local/origin 完全同步（0/0）。

### 9.3 ⚠️ refactor/staff-dedup-mcp 未合入——退役（关键发现）

merge-tree 预检发现 6 处冲突后逐文件 diff，结论：**该分支已被 main 完全取代，合入会造成 6 类回退**：

| 文件 | main 现状 | 分支旧版 | 合入后果 |
|---|---|---|---|
| ci.yml | 含 engine-tests + contract-tests 门禁 | 无这两个 job | 删除 CI 门禁 |
| vitest.config.ts | 主套件排除 engine + 独立 test:engine | CI 内选择性 include | 回退架构决策 |
| StaffDeck-main 4 文件 | 08-09 23:10 新版 | 08-09 13:14 旧版 | 回退到过期快照 |
| .gitignore | `/index.html`（修复版） | `index.html`（bug 版，误忽略 StaffDeck） | 引入已修 bug |
| src/（全仓 275 文件） | 含 contract 测试/confirmDialog/死代码已删 | 缺这些 | 丢 P2-1 门禁 + 丢新功能 + 复活死代码 |
| build-staffdeck-app.mjs | 与分支相同 | 同 | 无差异，无需合 |

分支 3 个 commit（0a54fc6b / a74c4f1a / 83c98e01）是"已合入内容的旧版重放"。已 `git branch -D` + `git push origin --delete` 退役，内容安全网 = `backup/wip-2026-08-04`。

> **教训**：合并前必须 merge-tree 预检 + 逐文件 diff 新旧方向，不能只看 `git log main..branch` 差集——差集 commit 可能是过期重放而非增量。

### 9.4 最终状态

- main = `5a5d4464`（v1.7.213 线），与 origin 完全同步（0 落后 0 领先）
- tracked 脏树归零；untracked 仅诊断脚本/截图/.tmp-logs（约定保留）
- 剩余分支：`backup/wip-2026-08-04`（安全网）+ `sync/openclaw-2026-08-04`（上游待审阅）

---

## 附：与历史交付物的关系

- **前报**：见《2026-08-10 整体分析与下一步开发方向》《2026-08-09 重新分析》
- **API 信封迁移**：见 `~/.workbuddy/skills/api-envelope-migration/SKILL.md`（含完整 skip 规则 + 手工迁移流程）
- **契约测试**：见 `tests/contracts/p21-envelope.contract.test.ts` + `apiContractSchema.ts`
- **上游差异**：见 `sync/openclaw-2026-08-04` 分支（401 文件待审阅）
- **数字员工**：见《2026-08-08-数字员工去iframe方案建议》（结论：维持 iframe）
- **rebase 坑**：见 08-12 日志 — SettingsPopover 嵌套菜单在 rebase 时被丢弃，补 fix commit 恢复
