# CrossWMS / CDFKnow 重新分析（2026-08-15）

> 承接《2026-08-13 整体分析与遗留问题》（停在 `01a6ab87` / v1.7.221）。
> 本报基于 2026-08-15 实时测量：git log / git status / 全 42 页 Card 扫描 / 行数统计 / 渠道与扩展盘点。
> 核心变化：**内置技能系统二期 12 commits 落地、微信/企业微信官方 SDK 渠道真实化、P1-2 视觉统一收口、tsgo 门禁实况修正、用户技能弹窗重构在飞**。

---

## 0. 一句话结论

**软件已从"整合期"完全进入"能力深化 + 体验打磨"阶段：双渠道（企微/公众号）官方 SDK 打通了真实收发闭环，内置技能系统完成启停全链路治理，P1-2 视觉债收口（仅剩 MetricsPage 待用户任务合入）。** 上一轮的两个高优遗留——上游分叉治理（缺 openclaw remote，暂缓）与 engine 测试门禁（CI 已挂硬门禁、本地体验割裂）——仍是唯二的结构性风险；当前最重要的工程动作是**把用户并行推进的技能弹窗重构安全合入 + 收口 MetricsPage 后做一次 1.7.224 发布验证**。

---

## 1. 当前状态快照（对比 08-13）

| 维度 | 08-13 报 | 本报（08-15） | 变化 |
|---|---|---|---|
| HEAD / 版本 | `d9170bd1` / v1.7.216 | **`487bbc43` / v1.7.224** | +8 版本 |
| 分支同步 | main 0/0 | **main 0/0** | 稳 ✅ |
| 自 08-13 提交数 | — | **29 commits**（含 2 次 bot bump、2 次 merge） | — |
| P1-2 UI 统一 | 12/42 页 | **全部收口**（42 页扫描仅剩规范内例外 + MetricsPage defer） | ✅ 完成 |
| 渠道接入 | demo 凭证 + demo 投递 | **企微/公众号官方 API 真实化**（探测校验/真实推送/回调解密） | ✅ 完成 |
| 内置技能 | 启停持久化后端就绪 | **全链路（工具列表/匹配/触发/执行四层拦截）+ 前端 OpenClaw 合并/中文词典/8 新技能** | ✅ 完成 |
| typecheck 门禁 | tsgo 增量（名不副实） | **typecheck:fast/server 改回真实 tsc + esbuild**（tsgo 与 TS7 不兼容实况修正） | ✅ 修复 |
| 上游治理 | runbook 已出、台账已建 | 仍缺 **openclaw remote**（暂缓，待用户提供 URL） | ⏸ 阻塞 |
| 在飞改动 | 4 处（metrics 等） | **用户技能弹窗重构**：12 文件改 + 4 新文件（SkillDialogShell 等） | ⚠️ 勿碰 |
| 服务器验证 | tsc EXIT=0 | **server tsc EXIT=0 · esbuild BUILD_SUCCESS · 渠道单测 16/16** | 稳 ✅ |

---

## 2. 已完成清单（滚动确认）

| 模块 | 状态 | 退出标准 |
|---|---|---|
| 双套 UI 收敛（39 MUI 员工页→StaffDeck iframe） | ✅ | `/staff /enterprise /workspace` → `/staffdeck` |
| git 瘦身（732M→89M） | ✅ | 全员已重 clone |
| API 信封迁移 P2-1 | ✅ 收口 | 6 批 / 25 文件 / 251 调用 + 契约测试 |
| P1 工程质量硬化 | ✅ | window.confirm→MUI Dialog、弹窗 z-index L0-L6、verify.mjs→Playwright |
| CI / tsc 门禁 | ✅ | **typecheck:fast/server 用真实 tsc(8GB)+esbuild；server tsconfig module=esnext**（2026-08-15 修正） |
| P1-2 UI 视觉统一 | ✅ 收口 | 42 页扫描：仅 KPI/条目/SOP 设计卡保留 + MetricsPage defer |
| **内置技能系统二期** | ✅ | 启停全链路四层拦截 + 运行时技能合并 + 前端 OpenClaw 合并/中文词典/图标推导 + 8 新技能（12 commits） |
| **微信/企业微信官方 SDK 渠道** | ✅ | extensions/wecom + extensions/wechat（client/send/crypto/probe/callback + 16 单测）+ 凭证探测 + 真实投递 + 回调解密 |
| 打包 seed fixture（新装无员工） | ✅ | package-mac-app.sh 拷贝 fixture + seedStaffDeck 路径兜底 |

---

## 3. 本轮重大变更盘点（08-13 → 08-15，29 commits）

### 3.1 内置技能系统二期（主线，12 commits：`008b5091..2acd9000`）
- **后端全链路启停生效**：`skillToolBridge`（AI 工具列表 + 执行前拦截，R2b-3）、`keywordTriggerEngine`（触发索引过滤）、`matchingService`（匹配过滤）三处应用 builtin status patches；`/api/user-skills` 合并 skillRegistry 运行时技能 + `executable` 标注 + `/rescan` 端点
- **前端**：SkillsPage 拉取 OpenClaw 技能合并进「内置」栏、`builtin-zh.ts` 中文词典（80+ 技能）、`builtin-skill-metadata.ts` 图标/触发词推导、Switch 启停持久化、SkillPreviewDialog 简化（+189/−464）
- **8 个新内置技能**：brainstorm / code-review / doc-writer / task-planner / translator / wms_inventory_check / wms_outbound_create / wms_transfer_create
- **R2b-4 安全**：停用技能不可经 URL 注入绑定（TopBarChatInput activeOnly）
- **会话切换修复**：ChatContext 消息保护（select-session 事件统一入口）
- 实测：`/api/user-skills` 152 技能、8 新技能全部 executable=true

### 3.2 微信 / 企业微信官方 SDK（2 commits：`27b2ba34` + `dcec8c05`）
- `extensions/wecom/`：官方 qyapi 客户端（access_token 缓存）、自建应用 message/send（text/markdown/image/file/news/textcard）+ 群机器人 webhook、**回调官方算法**（msg_signature 验签 + AES-256-CBC 解密 + URL 验证）、probe（gettoken+agent/get）、ChannelPlugin + ExtensionProvider
- `extensions/wechat/`：公众号 cgi-bin/token 客户端、客服消息 custom/send + 模板消息、明文/安全双模式验签 + AES 解密回调
- **渠道真实化**：wecom/wechat 凭证端点官方 API 探测通过才激活；`deliverToChannel` 真实 HTTP 推送（投递日志记录 status/error/external_id）；`channel-webhook` 企微回调解密真实化 + 新增公众号回调端点（配置读 DB 绑定，env 兜底）
- 单测 16/16（加密往返 / 签名 / URL 验证 / 明文与安全模式回调）

### 3.3 P1-2 收尾（`6a197f16`）
- SystemMonitorPage 7 个 section 容器 Card→Box 扁平化（扩展类型分布/消息生命周期/重试队列/系统信息/最慢操作/健康检查/通道状态），KPI 卡按规范保留
- **实测复核**：其余"复杂页"（TemplateMarket/MediaTools/MediaLibrary/Workflow/ImageGeneration）仅含条目/媒体类功能性卡，按规范保留 Card；WmsInboundPage 的 SOP 风格主卡属设计例外；PermissionsPage 3 卡为 KPI
- **结论：P1-2 除 MetricsPage（用户任务 defer）外全部完成**

### 3.4 工程门禁实况修正（`e1e27c1e` + `dfad014a`）
- **tsgo（@typescript/native-preview 7.0-dev）与两个 tsconfig 不兼容**（TS7 移除 baseUrl/node10）——记忆里"tsgo 已替代 tsc"是虚的；`typecheck:fast` = web tsc(8GB) + esbuild
- ESM 提交曾引入 server tsc TS1343 全灭（commonjs 不允许 import.meta）——`module: esnext` + 补缺失 import 修复，**server tsc 恢复 EXIT=0**

### 3.5 其他
- 打包 seed fixture（新装无数字员工修复）、release.json 1.7.222→1.7.224、软件分析报告刷新（用户）、上游 runbook/台账建立

---

## 4. 渠道与扩展生态现状

| 维度 | 实测 |
|---|---|
| 扩展目录总数 | **145 个**（provider 62 / utility 29 / channel 24 / tool 6 / web-search 6 / memory-host 3 / 其他 4） |
| 渠道扩展 | **24 个 channel 扩展**，含 feishu / **wecom（新）** / **wechat（新）** / discord / slack / telegram / qqbot / msteams 等 |
| 内置技能 | skills/ 91 个目录（含 8 个新中文 WMS/生产力技能） |
| 应用层规模 | src 181,342 行/606 文件 + server 非 engine 296,883 行 + packages 106,170 行 + extensions 425,520 行/1,635 文件 |
| 单测（非 engine） | 312 个测试文件 |
| engine 分叉 | server/engine 11,537 .ts / 272.9 万行（不变，占 server 90%） |

---

## 5. 四象限成熟度评估

| 象限 | 评级 | 判断 |
|---|---|---|
| **架构** | 🟢 中上 | 底座稳；双渠道官方 SDK 打通真实收发闭环；唯一结构性隐患仍是 **engine 272 万行硬分叉的治理机制**（无 openclaw remote，无法测真实距离） |
| **能力** | 🟢 完整 | 内置技能启停全链路、扩展生态 145 个、渠道 24 个、数字员工、跨境 WMS 域页面齐备 |
| **质量 / 工程** | 🟡 中 | tsc/esbuild/单测/CI 硬门禁就位；**engine 测试本地体验割裂**（851 unresolved imports，需先 build openclaw）；用户在飞改动致 web tsc 暂时 1 error（其测试文件） |
| **产品 / 方向** | 🟢 清晰 | 数字员工是产品赌注；本轮渠道真实化 + 技能系统补齐了"数字员工接入真实 IM"的最后一公里 |

---

## 6. 遗留问题清单（按等级）

| # | 问题 | 等级 | 现状 | 备注 |
|---|---|---|---|---|
| 1 | **上游分叉治理** | 🔴 高 | runbook/台账就绪，**缺 openclaw remote URL**（用户暂缓） | remote 连通后：fetch 真实分叉距离 → 首轮选择性 cherry-pick 演练 |
| 2 | **engine 测试门禁本地割裂** | 🟠 中 | CI 已有 engine-tests/contract-tests 硬门禁；本地 `test:engine` 851 unresolved imports | 须 CI 先 build openclaw；本地按 README 用 esbuild 校验 |
| 3 | **MetricsPage 视觉统一** | 🟡 中 | 用户 metrics 任务在飞（stash@{0} + 其工作树），28 处 Card | 待合入后收尾（大概率同 SystemMonitorPage：section 容器迁 Box，KPI 卡保留） |
| 4 | **在飞用户技能重构（勿碰）** | ⚠️ 注意 | 12 文件改 + 4 新文件（SkillDialogShell.tsx、3 测试）；web tsc 1 error 在其测试文件 | 提交/推送前须隔离保护 |
| 5 | 前端 WechatSetup 仍是扫码流程 | 🟡 中 | 公众号凭证表单未接（后端 `wechat/credentials` 端点已就绪） | StaffDeck-main 子模块补 credentials 模式 UI |
| 6 | 重复实现（markdown 渲染） | ✅ 已核验 | `MarkdownRenderer.tsx` 为唯一渲染器；`src/` 0 处 `dangerouslySetInnerHTML`（无原始 HTML 注入面） | 2026-08-15 |
| 7 | automation/trigger 双包 | 🟢 低 | DEFER | 协同重构风险>收益 |
| 8 | release.json 与 package.json 版本漂移 | 🟢 低 | bot 只 bump package.json；release.json 手动 | 发布时需手动对齐 |

---

## 7. 下一步方向（优先级路线图）

### P0 — 合入与收口（立即）
| 动作 | 退出标准 |
|---|---|
| **用户技能弹窗重构合入**：等待/协助其修完 `SkillPreviewDialog.keywordArea.test.tsx` 类型错误后，精确文件列表提交（不碰其他） | 工作树只剩可解释文件；web tsc EXIT=0 |
| **MetricsPage 收尾**：用户 metrics 任务合入后，section 容器迁 Box（KPI 卡保留） | P1-2 全 42 页一致 |
| **1.7.224 发布验证**：`build:mac:patch`（release.json 对齐 1.7.224）→ DMG + 全场景回归（_full_test.mjs） | 发布产物可用；46/46 回归绿 |

### P1 — 本迭代
| 动作 | 退出标准 |
|---|---|
| **渠道闭环打磨**：公众号前端凭证表单（WechatSetup credentials 模式）+ 企微回调真实环境联调 | 「渠道接入」全流程真实可用（含回调解密） |
| **数字员工能力闭环**：员工↔技能↔工具注册/发现标准沉淀 | 新员工接入 ≤ 半天 |
| 渠道 e2e：wecom/wechat 回调 mock 联调（加密请求→解析→事件总线） | 回调链路自动化验证 |

### P2 — 下迭代
- **上游分叉治理**（待 openclaw URL）：remote 配置 → 快照 → 首次 cherry-pick 演练（A 类直取/B 类适配层）
- engine 测试本地化（build openclaw 脚本化到 `test:engine` 前置）
- markdown 渲染单实现验证

### 可暂缓（记录不排期）
- 去 iframe / 回退 engine submodule / automation 协同重构：维持现状

---

## 8. 单一最重要下一步

> **把用户并行推进的技能弹窗重构安全合入（先修其测试文件类型错误），随后完成 MetricsPage 与 1.7.224 发布验证——让 main 工作树归零、门禁全绿，再进入上游治理。**
>
> 理由：① 在飞改动已膨胀到 12+4 文件且 web tsc 暂时 1 error，拖得越久越容易与后续发布/上游治理冲突；② 渠道 SDK 与技能系统刚完成，正是做一次真实发布（1.7.224）验证整体质量的好时机；③ 上游治理的阻塞是外部输入（openclaw URL），不依赖我方进度，可并行等待。

---

## 9. 路线图执行结果（2026-08-15 当日，全部落地）

| 项 | 结果 | 提交 |
|---|---|---|
| P0a 技能重构合入 | ✅ 修 5 处 eslint（4 空 catch 注释 + 1 未启用规则注释）；tsc 0 / 单测 114/114 | `fc5c4951` |
| P0b MetricsPage 收尾 | ✅ 5 section 容器迁移（KPI 保留）→ P1-2 全 42 页收口 | `d8706fb2` |
| P0c 发布验证 | ✅ 全量构建（tsc+vite+staffdeck）+ server_dist 启动 + 全场景回归 45/46（1 warn 非阻塞）；release.json 对齐 1.7.225 | `23c6aaf3` + .app 组装（打包中） |
| P2a 上游治理 | ✅ openclaw remote 配置 + 真实分叉距离实测（非测试共享文件仅 5.6% 相同）+ 台账回填 + 首次 A 类演练（120 commits 仅 1 候选、no-op） | `f5b648a4` |
| P1a 公众号凭证表单 | ✅ WechatSetup 新增 credentials 模式（AppID/Secret/Token/AESKey/openid，走官方 API 探测） | `7ed1dfa3` |
| P1b 渠道回调 e2e | ✅ 企微/公众号 8 用例全绿（加密→端点→验签解密→eventBus） | `ad323006` |
| P1c 数字员工闭环 | ✅ /capabilities 能力清单端点 + 标准文档（新员工接入 ≤ 半天） | `289687d7` |
| P2b engine 测试本地化 | ✅ 核实 CI 已收口（build openclaw dist 优先硬门禁 + mock 兜底），本地无需手动构建 | —（核验） |
| P2c markdown 渲染 | ✅ 单实现确认：MarkdownRenderer.tsx 为唯一渲染器；src/ 0 处 dangerouslySetInnerHTML | —（核验） |

> 待收尾：DMG 打包（build:mac:patch → 1.7.226 发布）与「能力清单前端展示」（StaffDeck 员工档案页）。

---

## 附：测量方法与排除项
- 行数：`find <dir> -name '*.ts' -o -name '*.tsx' | grep -v node_modules | xargs wc -l`，`awk '$2=="total"{s+=$1}END{print s}'`（防多 total 误报）；排除 node_modules / dist* / server_dist / coverage / .git / engine
- P1-2 扫描：`grep -c '<Card \|<Card>\|CardContent' src/pages/*.tsx`（42 页全量）
- 时间窗：2026-08-13 报基线 `01a6ab87`（v1.7.221）→ 现 HEAD `487bbc43`（v1.7.224）
- 关联交付物：《2026-08-13 整体分析与遗留问题》《2026-08-13 上游分叉治理runbook》《UPSTREAM_SYNC.md》、`.workbuddy/memory/` 日报
