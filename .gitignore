# Dependencies
node_modules/
server/node_modules/

# Build outputs
dist/
build/
build-pywebview/
release/
*.dmg

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
*.egg
venv/
/env/

# Electron
.out/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env.*
.python-version

# === 安全：证书与私钥（禁止提交仓库，吊销操作见 scripts/revoke-and-rotate-certs.zsh）===
certs/
*.key
*.p12
*.pfx
*.csr
*.srl
# 允许公开的证书分发：如需提交根证书请使用 .pub.crt 命名
*.crt
!*.pub.crt
*.pem
!*.pub.pem

# Logs
*.log
npm-debug.log*

# OpenClaw (git submodule) — do not ignore, tracked via .gitmodules
openclow-ai-computer-control/

# Misc
version.txt

# Electron (deprecated)
electron/
dist-electron/
main.js
entitlements.mac.plist

# Old build scripts
build-dmg.sh
build-dmg/
bump-version.cjs
CrossWMS.spec

# Manual .app assembly (pywebview OOM workaround)
CrossWMS.app/

# Docs (generated)
/docs/

# Root index.html (generated by some build)
index.html

# Preview images
preview.png

# PostCSS config (if not used)
postcss.config.js

# Test / temp files
start-mac.sh
test-app/
test-electron.cjs
test-main.cjs
dist-server/
# esbuild 单文件产物（build-server.mjs 输出，dev:fast 直接 node 它）。
# 2026-08-04：此前漏忽略，导致 31MB 的 index.cjs 被跟踪，git 历史已累积 6 个版本 ≈144MB，
# push 时触发 GitHub「文件超过 50MB 推荐上限」警告。
server_dist/
test-results/
e2e-results/
e2e/e2e-results/
e2e/e2e-report/
coverage/
.vitest-cache/

# StaffDeck 各子项目需要独立安装依赖，但源码文件必须跟踪
StaffDeck-main/**/node_modules/
StaffDeck-main/**/.next/
StaffDeck-main/**/dist/
StaffDeck-main/**/.turbo/
StaffDeck-main/**/.output/
daemon.pid
.workbuddy/

# Desktop app build artifacts
dist-app/
apps/macos/.build/
apps/macos/Package.resolved

# Package lock
# package-lock.json
.wip-unfinished/
.dev-data/
